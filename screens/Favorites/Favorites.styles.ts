import { StyleSheet, Dimensions } from "react-native";

const styles = StyleSheet.create({
  pageContain: {
    flex: 1,
  },
  bgImg: {
    height: "100%",
    width: "100%",
    position: "relative",
  },
});

export default styles;
